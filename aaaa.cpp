#include <iostream>
#include <fstream>
#include <string>
#include <vector>
#include <sstream>
#include <sys/stat.h>

int main(){    
    do{ 
        std::cout << "Press the 'enter' key to continue.." << '\n';
        std::cin.ignore();   
    }while(0);
    return 0;
}